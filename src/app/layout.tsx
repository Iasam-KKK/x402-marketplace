import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import { Providers } from "@/components/Providers";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "x402 API Marketplace | Pay-Per-Use APIs",
  description:
    "Discover and use powerful APIs with instant micropayments. No subscriptions, no accounts - just pay per request using the x402 protocol.",
  keywords: ["API", "marketplace", "x402", "micropayments", "crypto", "web3"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.variable} font-sans antialiased`}>
        <Providers>{children}</Providers>
      </body>
    </html>
  );
}
